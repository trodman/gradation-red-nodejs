function setup() {
  createCanvas( windowWidth, windowHeight )

  //drums = EDrums('x*o*x*o-')
  //follow = Follow( drums )
  Gibber.scale.mode = 'Chromatic'
  a = SoundFont('piano').note.seq( [0,1,2,3], 1/4 )
}

function draw() {
  //background( follow.getValue() * 255 )
}